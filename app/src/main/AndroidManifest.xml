<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.petra.healthylifeapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <permission
        android:name="com.maret.maps.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.maret.maps.permission.MAPS_RECEIVE" />

    <uses-feature android:name="android.hardware.location.gps" />
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:name=".HealthyLifeApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <uses-library android:name="com.google.android.maps" />

        <service
            android:name=".SensorService"
            android:enabled="true"></service>

        <!--<receiver-->
        <!--android:name=".SensorRestarterBroadcastReceiver"-->
        <!--android:enabled="true"-->
        <!--android:exported="true"-->
        <!--android:label="RestartServiceWhenStopped">-->
        <!--<intent-filter>-->
        <!--<action android:name="uk.ac.shef.oak.ActivityRecognition.RestartSensor"/>-->
        <!--</intent-filter>-->
        <!--</receiver>-->

        <receiver android:name=".SensorService$MyBroadCastReciever">
            <intent-filter>
            <action android:name="android.intent.action.SCREEN_ON" />
            <action android:name="android.intent.action.SCREEN_OFF" />
            <!--<action android:name="android.intent.action.USER_PRESENT" />-->
            </intent-filter>
        </receiver>

        <receiver android:name=".SensorService$NotificationReceiver"></receiver>

        <activity
            android:name=".MainActivity"
            android:parentActivityName=".ConfigureProfile">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <!--<meta-data-->
            <!--android:name="android.support.PARENT_ACTIVITY"-->
            <!--android:value=".FirebaseLogin" />-->

            <!--<meta-data-->
            <!--android:name="android.support.PARENT_ACTIVITY"-->
            <!--android:value=".ConfigureProfile" />-->
        </activity>
        <activity
            android:name=".FirebaseLogin"
            android:label="@string/title_activity_firebase_login"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".ViewHistoryAPI"
            android:parentActivityName=".MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />

        </activity>
        <activity
            android:name=".Maps"
            android:label="@string/title_activity_show_maps"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ViewHistoryAPI" />
        </activity>
        <activity
            android:name=".ConfigureProfile"
            android:parentActivityName=".FirebaseLogin"
            android:theme="@style/AppTheme.NoActionBar">
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".FirebaseLogin" />
        </activity>

        <service android:name=".ActivityRecognizedService" />
        <service android:name=".BackgroundService" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.google.android.awareness.API_KEY"
            android:value="@string/google_play_services_key" />

        <!-- places/location declaration -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_play_services_key" />

        <!-- Beacon snapshots/fences declaration -->
        <meta-data
            android:name="com.google.android.nearby.messages.API_KEY"
            android:value="@string/google_play_services_key" />

        <!-- <activity -->
        <!-- android:name=".Login" -->
        <!-- android:label="@string/title_activity_firebase_login" -->
        <!-- android:theme="@style/AppTheme.NoActionBar"></activity> -->

        <activity
            android:name=".Login"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--<activity-->
        <!--android:name=".ConfigureProfile"-->
        <!--android:label="@string/app_name"-->
        <!--android:theme="@style/AppTheme.NoActionBar">-->
        <!--<intent-filter>-->
        <!--<action android:name="android.intent.action.MAIN" />-->

        <!--<category android:name="android.intent.category.LAUNCHER" />-->
        <!--</intent-filter>-->
        <!--</activity>-->

        <activity
            android:name=".CalendarActivity"
            android:parentActivityName=".MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

    </application>


</manifest>